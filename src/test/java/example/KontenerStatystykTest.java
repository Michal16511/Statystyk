/*package example;

import Postacie.Postac;
import Postacie.Trener;
import michal.rys.commons.ConsoleScannerSingleton;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.BDDMockito;
import org.powermock.api.mockito.PowerMockito;
import org.powermock.core.classloader.annotations.PrepareForTest;
import org.powermock.modules.junit4.PowerMockRunner;

import java.util.LinkedList;

import static org.junit.Assert.assertEquals;

/**
 * Created by Michal on 2016-07-07.
 */
//@RunWith(PowerMockRunner.class)
//@PrepareForTest(ConsoleScannerSingleton.class)
//public class KontenerStatystykTest {

    //private KontenerStatystyk kontenerStatystyk = new KontenerStatystyk();
   // @Test
    //public void shouldAddNewTrainer(){

      //  assertEquals(0, kontenerStatystyk.listaZawodnikow.size());

       // PowerMockito.mockStatic(ConsoleScannerSingleton.class);
       // BDDMockito.given(ConsoleScannerSingleton.getConsoleScanner().nextLine()).willReturn(LinkedList < Postac > listaZawodnikow = new LinkedList<Postac>());

        //symuluje ze wciskam 1 czylitrener

      //  kontenerStatystyk.dodajZawodnika();

        //sprawdzam czy lista zwiekszyla sie o 1

      //  assertEquals(1, kontenerStatystyk.listaZawodnikow.size());

 //   }
//}*/